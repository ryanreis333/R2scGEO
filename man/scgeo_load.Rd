% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{scgeo_load}
\alias{scgeo_load}
\title{Load scRNA-seq data from any GEO file or directory}
\usage{
scgeo_load(
  path,
  as = c("matrix", "seurat", "sce"),
  sample = NULL,
  project = "R2scGEO"
)
}
\arguments{
\item{path}{A file or directory (e.g. the output of \code{\link[=scgeo_download]{scgeo_download()}}).}

\item{as}{Output type: "matrix" (sparse \code{dgCMatrix}, default), "seurat",
or "sce".}

\item{sample}{Optional name/regex to pick one triplet when a directory or
archive contains several samples. If omitted and multiple are found, a
named list is returned.}

\item{project}{Project name passed to Seurat (default "R2scGEO").}
}
\value{
A \code{dgCMatrix}, \code{Seurat}, or \code{SingleCellExperiment} — or a named list
of these when multiple samples are present.
}
\description{
Auto-detects the storage format and reads it into the object you ask for.
Handles 10x Matrix Market triplets, CellRanger \code{.h5}, AnnData \code{.h5ad},
\code{.loom}, serialized \code{.rds} (Seurat / SingleCellExperiment / matrix), dense
delimited tables (\code{.csv} / \code{.tsv} / \code{.txt}, optionally gzipped), and
\code{.tar} / \code{.zip} archives (which are extracted and scanned recursively).
}
\examples{
\dontrun{
files <- scgeo_download("GSE164897")
obj <- scgeo_load(dirname(files[1]), as = "seurat")
}
}
